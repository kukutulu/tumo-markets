import localFont from 'next/font/local';

export const LexendFont = localFont({
  src: '../../public/fonts/Lexend-Regular.ttf',
  variable: '--font-lexend',
});

export const solanaNetwork = 'mainnet';
